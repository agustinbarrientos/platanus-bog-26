fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
# Email validation and normalisation in the signup and login models.
email-validator>=2.2
httpx>=0.27
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30
# argon2id password hashing. 23.1 is where InvalidHashError got its name.
argon2-cffi>=23.1
# Vectorized biomarker random walk in the Monte Carlo simulation.
numpy>=2.0
# Reporte de salud descargable (POST /me/health-context/reporte.pdf): PDF en
# memoria con la paleta/tipografía de la app (fuentes TTF en app/report/fonts).
reportlab>=4.2
# The health-context chat agent (POST /me/health-context/chat).
anthropic>=1.0
# Multipart form parsing — required by FastAPI's UploadFile/File().
python-multipart>=0.0.9
# Tests: `pytest` from apps/backend (tests/ cover the chat retriever and the
# chat router with a fake Anthropic client — no network, no database).
pytest>=8.0
