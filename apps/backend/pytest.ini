[pytest]
# Run from apps/backend: `pytest` (or one test: `pytest tests/test_chat_rag.py::test_x`).
# pythonpath makes `import app` work without installing the package.
pythonpath = .
testpaths = tests
